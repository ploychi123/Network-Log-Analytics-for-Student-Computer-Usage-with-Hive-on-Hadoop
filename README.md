# Network-Log-Analytics-for-Student-Computer-Usage-with-Hive-on-Hadoop
## 📌Overview (ภาพรวมของโปรเจกต์)
โปรเจกต์นี้เป็นการวิเคราะห์ข้อมูล Network Logs ของนักศึกษาในมหาวิทยาลัย ซึ่งประกอบด้วยข้อมูลสำคัญ เช่น Timestamp, IP Address, Port, HTTP Method, Host และ URI เพื่อตรวจสอบรูปแบบการใช้งานอินเทอร์เน็ต และพฤติกรรมการเข้าถึงบริการต่าง ๆ

ในการประมวลผลข้อมูลขนาดใหญ่ ได้ใช้ Hive บน Hadoop เพื่อ
- สร้างตารางแยกตามรูปแบบไฟล์ (HTTP/HTTPS)
- โหลดข้อมูลจำนวนมากเข้าสู่ระบบ Distributed Storage
- ใช้คำสั่ง SQL บน Hive เพื่อค้นหา จัดกลุ่ม และสรุปข้อมูลที่เกี่ยวข้อง

วัตถุประสงค์หลักคือการตรวจสอบรูปแบบการใช้งานเว็บไซต์ และสรุปจำนวนการเข้าถึงบริการสำคัญ เช่น Google, Microsoft, TeamViewer และ Proxy ภายในมหาวิทยาลัย

## 📁Dataset
ข้อมูลที่ใช้ในโปรเจกต์นี้เป็น ชุดข้อมูลจำลอง (Mockup Dataset) ที่สร้างขึ้นเพื่อเลียนแบบโครงสร้างของ Network Logs จริงในมหาวิทยาลัย โดยไม่ได้มีการใช้ข้อมูลส่วนบุคคลหรือข้อมูลจริงของผู้ใช้งานแต่อย่างใด ชุดข้อมูลถูกสร้างขึ้นใหม่ 100% ด้วยเครื่องมือ Mockaroo เพื่อให้สามารถสาธิตกระบวนการวิเคราะห์ใน Hadoop และ Hive ได้อย่างสมจริง

ข้อมูลจำลองถูกแยกออกเป็น 2 ประเภทตามรูปแบบโปรโตคอลที่มักพบในระบบเครือข่าย ได้แก่ HTTP และ HTTPS ซึ่งแต่ละประเภทมีโครงสร้างคอลัมน์แตกต่างกัน ดังนี้:

1. HTTP Log Dataset (Mock Data)
ชุดข้อมูลนี้จำลองลักษณะของ HTTP traffic ที่พบในระบบเครือข่ายจริง โดยประกอบด้วย 6 คอลัมน์หลัก ได้แก่:

| คอลัมน์           | ความหมาย                                                                                                    |
| ----------------- | ----------------------------------------------------------------------------------------------------------- |
| **ipaddr_origin** | IP Address ของคอมพิวเตอร์ต้นทาง                                                                             |
| **idp_origin**    | Port ที่เครื่องต้นทางใช้ (random high ports)                                                                       |
| **ipaddr_resp**   | IP Address ของเซิร์ฟเวอร์ปลายทาง                                                                            |
| **idp_resp**      | Port ปลายทาง (เช่น 80, 8080)                                                                             |
| **host**          | ชื่อเว็บไซต์ เช่น google.com, facebook.com                                                                    |
| **uri**           | Path หรือ resource บนเว็บไซต์ เช่น /login, /search?q=data |

2. HTTPS Log Dataset (Mock Data)
ชุดข้อมูลนี้จำลอง HTTPS traffic ที่ใช้ในเว็บไซต์ที่มีการเข้ารหัส เช่น Google, Microsoft หรือระบบภายในองค์กร โดยมี 5 คอลัมน์ ได้แก่:

| คอลัมน์           | ความหมาย                                                              |
| ----------------- | --------------------------------------------------------------------- |
| **ipaddr_origin** | IP Address ของเครื่องต้นทาง                                           |
| **idp_origin**    | Port ของเครื่องต้นทาง                                                 |
| **ipaddr_resp**   | IP Address ของปลายทาง                                                 |
| **idp_resp**      | มักเป็น Port 443 (HTTPS)                                   |
| **host**          | ชื่อโดเมน เช่น google.com, teams.microsoft.com, zoom.us |


