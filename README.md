# Network-Log-Analytics-for-Student-Computer-Usage-with-Hive-on-Hadoop
## 📌Overview (ภาพรวมของโปรเจกต์)
โปรเจกต์นี้เป็นการวิเคราะห์ข้อมูล Network Logs ของนักศึกษาในมหาวิทยาลัย ซึ่งประกอบด้วยข้อมูลสำคัญ เช่น Timestamp, IP Address, Port, HTTP Method, Host และ URI เพื่อตรวจสอบรูปแบบการใช้งานอินเทอร์เน็ต และพฤติกรรมการเข้าถึงบริการต่าง ๆ

ในการประมวลผลข้อมูลขนาดใหญ่ ได้ใช้ Hive บน Hadoop เพื่อ
- สร้างตารางแยกตามรูปแบบไฟล์ (HTTP/HTTPS)
- โหลดข้อมูลจำนวนมากเข้าสู่ระบบ Distributed Storage
- ใช้คำสั่ง SQL บน Hive เพื่อค้นหา จัดกลุ่ม และสรุปข้อมูลที่เกี่ยวข้อง

วัตถุประสงค์หลักคือการตรวจสอบรูปแบบการใช้งานเว็บไซต์ และสรุปจำนวนการเข้าถึงบริการสำคัญ เช่น Google, Microsoft, TeamViewer และ Proxy ภายในมหาวิทยาลัย

## 📁Dataset
ชุดข้อมูลที่ใช้ในโปรเจกต์นี้เป็น Network Log Files ที่ถูกเก็บจากคอมพิวเตอร์ของนักศึกษาในมหาวิทยาลัย โดยมีการแยก Log ออกเป็น 2 ประเภทหลัก ได้แก่ HTTP และ HTTPS ซึ่งแต่ละประเภทจะมีจำนวนคอลัมน์ไม่เท่ากัน จึงต้องใช้การสร้างตาราง (Hive Table) แยกกันในการประมวลผล

1. HTTP Log Dataset (ไฟล์ http1–http4)
กลุ่มนี้ประกอบด้วยไฟล์ข้อความ (Text File) หลายไฟล์ เช่น http1, http2, http3, http4 ซึ่งรวมกันแล้วมีหลายแสนแถว ขนาดใหญ่หลาย MB

| คอลัมน์           | ความหมาย                                                                                                    |
| ----------------- | ----------------------------------------------------------------------------------------------------------- |
| **ipaddr_origin** | IP Address ของคอมพิวเตอร์ต้นทาง                                                                             |
| **idp_origin**    | Port ของเครื่องต้นทางที่ส่งข้อมูลออก                                                                        |
| **ipaddr_resp**   | IP Address ของเซิร์ฟเวอร์ปลายทาง                                                                            |
| **idp_resp**      | Port ของเซิร์ฟเวอร์ปลายทาง                                                                                  |
| **host**          | ชื่อเว็บไซต์หรือโดเมนที่ถูกเข้าถึง                                                                          |
| **uri**           | เส้นทางของ resource ที่ถูกเรียก (หากเซิร์ฟเวอร์มี path ต่อท้าย เช่น `/login`, `/home`, `/resource` เป็นต้น) |

2. HTTPS Log Dataset (ไฟล์ https1–https4)
เป็นไฟล์ที่มาจากการเข้าใช้งานผ่านโปรโตคอล HTTPS (Port 443) ซึ่งมีความปลอดภัยสูงกว่า HTTP

| คอลัมน์           | ความหมาย                                                              |
| ----------------- | --------------------------------------------------------------------- |
| **ipaddr_origin** | IP Address ของเครื่องต้นทาง                                           |
| **idp_origin**    | Port ของเครื่องต้นทาง                                                 |
| **ipaddr_resp**   | IP Address ของปลายทาง                                                 |
| **idp_resp**      | Port ของปลายทาง (มักจะเป็น 443)                                       |
| **host**          | โดเมนของเว็บไซต์ เช่น `google.com`, `microsoft.com`, `teamviewer.com` |


